<!--
  formatter — paste this inside <build><plugins>...</plugins> in pom.xml.
  Replace <!-- PROFILE_FILE --> with the relative path to the profile XML you
  copied into your project (for the default profile:
      config/java-alipay-formatter.xml
  ).
-->
<plugin>
  <groupId>com.diffplug.spotless</groupId>
  <artifactId>spotless-maven-plugin</artifactId>
  <version>2.43.0</version>
  <configuration>
    <java>
      <includes>
        <include>src/main/java/**/*.java</include>
        <include>src/test/java/**/*.java</include>
      </includes>
      <eclipse>
        <version>4.26</version>
        <file>${project.basedir}/<!-- PROFILE_FILE --></file>
      </eclipse>
      <removeUnusedImports/>
      <trimTrailingWhitespace/>
      <endWithNewline/>
    </java>
  </configuration>
  <!--
    Optional: make `mvn verify` fail if formatting is off. The PostToolUse
    hook already runs `spotless:apply`, so this is mostly useful for CI.
  -->
  <!--
  <executions>
    <execution>
      <goals><goal>check</goal></goals>
    </execution>
  </executions>
  -->
</plugin>
